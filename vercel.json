{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bun run --cwd apps/landing build",
  "installCommand": "bun install --frozen-lockfile",
  "outputDirectory": "apps/landing/dist"
}
