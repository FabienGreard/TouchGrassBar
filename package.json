{
  "name": "touchgrassbar",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bunx --bun turbo run build",
    "dev": "bunx --bun turbo run dev",
    "doctor": "bunx --bun react-doctor@0.9.3 --yes --blocking warning --no-score",
    "doctor:score": "bunx --bun react-doctor@0.9.3 --yes --json --json-compact",
    "lint": "bunx --bun turbo run lint",
    "test": "bunx --bun turbo run test",
    "typecheck": "bunx --bun turbo run typecheck",
    "quality": "bun run contracts:check && bun run quality:code",
    "quality:code": "bun run lint && bun run typecheck && bun run test && bun run build && bun run doctor",
    "convex:dev": "bun scripts/convex-local.ts dev",
    "convex:setup:local": "bun scripts/convex-local.ts setup",
    "convex:codegen": "bunx --bun convex codegen",
    "convex:ai:status": "bunx --bun convex ai-files status",
    "contracts:generate": "bun scripts/generate-native-contract.ts",
    "contracts:check": "bun scripts/generate-native-contract.ts --check",
    "desktop": "bun scripts/run-desktop-dev.ts",
    "desktop:preview": "bun scripts/run-desktop-dev.ts --browser",
    "desktop:clean": "cargo clean --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "desktop:build": "bun run desktop:clean && bun run --cwd apps/desktop tauri build --bundles app",
    "desktop:reset-local": "bun scripts/reset-desktop-local.ts",
    "landing": "bun run --cwd apps/landing dev",
    "storybook:ui": "bun run --cwd packages/ui storybook",
    "storybook:ui:build": "bun run --cwd packages/ui build-storybook",
    "worktree:setup": "bun install --frozen-lockfile && bun run convex:setup:local"
  },
  "devDependencies": {
    "convex": "1.43.0",
    "oxlint": "1.76.0",
    "react-doctor": "0.9.3",
    "turbo": "2.10.8",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
