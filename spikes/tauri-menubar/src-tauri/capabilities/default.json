{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the native viability spike",
  "windows": ["panel", "settings"],
  "permissions": ["core:default"]
}
