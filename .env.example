# Convex writes these values when a deployment is selected.
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=

# Optional when more than one matching local signing resource is installed.
TOUCHGRASS_DEV_SIGNING_IDENTITY=
TOUCHGRASS_DEV_PROVISIONING_PROFILE=
